/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "upcdn.io",
      "res.cloudinary.com",
      "firebasestorage.googleapis.com",
    ],
  },
};

export default nextConfig;
